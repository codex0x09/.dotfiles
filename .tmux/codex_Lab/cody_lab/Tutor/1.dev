#!/bin/bash

# split window vertically into two panes 
# with bottom section being 20% of the total "Heigt"
# the flag -p stands for pane-size
tmux split-window -v -p 20 

# split window horizontally into two panes 
# with right section being 50% of the total "Width"
tmux split-window -h -p 50
